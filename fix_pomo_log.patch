--- src/app/(app)/timer/actions.ts
+++ src/app/(app)/timer/actions.ts
@@ -58,7 +58,8 @@
   const { error: pomoError } = await supabase.from('pomodoro_logs').insert({
     student_uuid: user.id,
     subject,
-    duration: minutes
+    duration_seconds: minutes * 60,
+    event_type: 'completed'
   });
 
   // 2. student_activity_logs に記録（新機能用）
